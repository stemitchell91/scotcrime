<h3>
  Police Strength
</h3>

<p>
  The number of Full Time Equivalent Police Officers employed in Scotland in each quarter.
</p>

<table border="1">
  <thead>
    <tr>
      <th>Period</th>
      <th>Full Time Equivalents</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @statistics.where(name: "policedata").find_each do |statistic| %>
      <tr>
        <td><%= statistic.year %></td>
        <td><%= statistic.number1.to_i %></td>
        <td><%= link_to 'Destroy', statistic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>