<h3>
  Sentencing
</h3>

<p>
  The total number of people with a charge proved in a Scottish Court, sorted by type of court.
</p>

<table border="1">
  <thead>
    <tr>
      <th>Year</th>
      <th>High Court</th>
      <th>Sheriff Solemn</th>
      <th>Sheriff Summary</th>
      <th>JP Court</th>
      <th>All Courts</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @statistics.where(name: "sentencing").find_each do |statistic| %>
      <tr>
        <td><%= statistic.year %></td>
        <td><%= statistic.number1.to_i %></td>
        <td><%= statistic.number2.to_i %></td>
        <td><%= statistic.number3.to_i %></td>
        <td><%= statistic.number4.to_i %></td>
        <td><%= statistic.number5.to_i %></td>
        <td><%= link_to 'Destroy', statistic, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>